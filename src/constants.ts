export const LEFT_CLICK = 'left';

export enum WaitMs {
  min = 20,
  basic = 100,
  half = 500,
  sec = 1000,
};

export enum MouseButtonState {
  down = 'down',
  up = 'up',
};

export enum VikHotKeys {
  watchtower = 'w',
  clans = 'c',
  resourceAid = 's',
  loki = 'q',
  barracks = 't',
};
